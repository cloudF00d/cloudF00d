<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ohgiraffers.semiproject.manager.search.model.dao.SearchUserMapper">

    <resultMap id="generalResultMap" type="UserDTO">
        <id property="userCode" column="USER_CODE"/>
        <result property="userId" column="USER_ID"/>
        <result property="password" column="PASSWORD"/>
        <result property="email" column="EMAIL"/>
        <result property="registrationDate" column="REGISTRATION_DATE"/>
        <result property="address" column="ADDRESS"/>
        <result property="userName" column="USER_NAME"/>
        <result property="dateBirth" column="DATE_BIRTH"/>
        <result property="phone" column="PHONE"/>
        <result property="activityStatus" column="ACTIVITY_STATUS"/>
        <result property="reportCount" column="REPORT_COUNT"/>
        <result property="couponStatus" column="COUPON_STATUS"/>
    </resultMap>
    
    <resultMap id="userInfo" type=""

    <select id="findAllUser" resultMap="generalResultMap">
        SELECT
            A.USER_CODE,
            A.USER_ID,
            A.PASSWORD,
            A.EMAIL,
            A.REGISTRATION_DATE,
            A.ADDRESS,
            A.USER_NAME,
            A.DATE_BIRTH,
            A.PHONE,
            A.ACTIVITY_STATUS,
            A.REPORT_COUNT,
            A.COUPON_STATUS
        FROM
            TBL_USER A

    </select>

    <select id="userDetail" resultMap="generalResultMap">

    </select>
</mapper>